import 'package:aeyrium_clock/aeyrium_clock.dart';
import 'package:flutter/material.dart';
import 'package:flutter_clock_helper/model.dart';
import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('Assets are loaded and clock painter built',
      (WidgetTester tester) async {
    // final notifier = ClockNotifier();
    // await tester.runAsync(() async {
    //   await notifier.loadAssets();
    // });
    // await tester.pumpWidget(AeyriumClock(ClockModel(), notifier));
    // final custPaintFinder = find.byKey(Key('digital_painter'));
    // expect(custPaintFinder, findsOneWidget);
  });
}
